#include <stdio.h>
#define ARRAY_SIZE 100
int main() {
  int i, j;
  int A[ARRAY_SIZE] = {0};

  for (i = 2; i < ARRAY_SIZE; i++){
      A[i] = 1;
  }
  
  for (int i = 0; i < ARRAY_SIZE; i++ ) {
    for (int j = 0; j < ){
      if (A[i] == )
    }

  }
  for (i = 2; i < ARRAY_SIZE; i++){
    printf("%d ", A[i]  );
  }

  printf("\n");

  return 0;
}
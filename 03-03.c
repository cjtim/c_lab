#include <stdio.h>
#include <stdlib.h>
int main(){
    int i  , max, a, b;
    char a_str[20],b_str[20];
    fgets(a_str,20,stdin);
    fgets(b_str,20,stdin);
    a = atoi(a_str);
    b = atoi(b_str);
    
}